import React from "react";
import { HOME } from "../constants";

const Banner = () => {
  return (
    <div className="banner-image">
      <h3>{HOME.BANNER_TITLE}</h3>
    </div>
  );
};

export default Banner;
